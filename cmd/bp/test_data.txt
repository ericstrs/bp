	/*
		// ---- Daily TODO List -----
		task1 := new(t.Task)
		t1 := new(t.TodoTask)
		t1.SetTask(task1)
		t1.SetDescription("then what? this is a really long sentence.")
		t1.SetShowDesc(true)
		t1.SetName("code")
		t1.SetStarted(time.Now())
		t1.SetDone(true)
		t1.SetPriority(0)

		task2 := new(t.Task)
		t2 := new(t.TodoTask)
		t2.SetTask(task2)
		t2.SetStarted(time.Now().Add(-24 * time.Hour))
		t2.SetName("read")
		t2.SetPriority(1)

		task3 := new(t.Task)
		t3 := new(t.TodoTask)
		t3.SetTask(task3)
		t3.SetName("eat")
		t3.SetDone(true)
		t3.SetStarted(time.Now())
		t3.SetPriority(2)

		list := new(t.TodoList)
		list.Add(t1, 0)
		list.Add(t2, 1)
		list.Add(t3, 2)
		// -------------------------

			todoColumn1 := new(t.BoardColumn)
			todoColumn1.SetTitle("TODO")
			p := new(t.Task)
			pkms := new(t.BoardTask)
			pkms.SetTask(p)
			pkms.SetName("Build PKMS")
			pkms.SetDescription("Build a Personal Knowledge Management System.")
			pkms.SetStarted(time.Now())
			pkms.SetPriority(0)
			pkms.SetHasChild(false)
			pkms.SetChild(&t.Board{})
			todoColumn1.Add(pkms)

			todoingColumn := new(t.BoardColumn)
			todoingColumn.SetTitle("TODO")
			nt := new(t.Task)
			tot := new(t.BoardTask)
			tot.SetTask(nt)
			tot.SetName("Plan phase")
			tot.SetStarted(time.Now())
			tot.SetPriority(0)
			tot.SetChild(&t.Board{})
			tot.SetHasChild(false)
			todoingColumn.Add(tot)
			child := new(t.Board)
			child.SetTitle("Build project management software")
			child.SetParentBoard(&t.Board{})
			child.SetParentTask(&t.BoardTask{})
			child.AddColumn(todoingColumn)

			workingOnColumn1 := new(t.BoardColumn)
			workingOnColumn1.SetTitle("Working On")
			m := new(t.Task)
			man := new(t.BoardTask)
			man.SetTask(m)
			man.SetName("Build project management software")
			man.SetDescription("This consists of a basic TODO list and rooted kanban trees, mixed together.")
			man.SetStarted(time.Now())
			man.SetPriority(0)
			man.SetHasChild(true)
			man.SetChild(child)
			workingOnColumn1.Add(man)

			doneColumn1 := new(t.BoardColumn)
			doneColumn1.SetTitle("Done")
			n := new(t.Task)
			nut := new(t.BoardTask)
			nut.SetTask(n)
			nut.SetName("Build nutrition program")
			nut.SetDescription("This consist of creating an app for food logging and nutritional feedback.")
			nut.SetStarted(time.Now())
			nut.SetPriority(0)
			nut.SetChild(&t.Board{})
			nut.SetHasChild(false)
			doneColumn1.Add(nut)

			root1 := new(t.Board)
			root1.SetTitle("Deep Work")
			root1.SetParentBoard(&t.Board{})
			root1.SetParentTask(&t.BoardTask{})
			root1.AddColumn(todoColumn1)
			root1.AddColumn(workingOnColumn1)
			root1.AddColumn(doneColumn1)
			root1.AddChild(child)

			root2 := new(t.Board)
			root2.SetTitle("Evening Work")
			root2.SetParentBoard(&t.Board{})
			root2.SetParentTask(&t.BoardTask{})

			var boards []*t.Board
			boards = append(boards, root1, root2)

		tree := new(t.BoardTree)
		//	tree.AddRoot(root1)
		//	tree.AddRoot(root2)
	*/
